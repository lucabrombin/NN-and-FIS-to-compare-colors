if L is scuro then newDE is uguali;
if L is medio && C is saturo && h is giallo && DE is inesperto then newDE is esperto;
if L is medio && C is saturo && h is giallo && DE is visibile then newDE is inesperto;
if L is medio && C is saturo && h is giallo && DE is diverso then newDE is inesperto;
if L is medio && C is saturo && h is blu && DE is esperto then newDE is inesperto;
if L is medio && C is saturo && h is blu && DE is inesperto then newDE is visibile;
if L is medio && C is saturo && h is viola && DE is esperto then newDE is inesperto;
if L is medio && C is saturo && h is viola && DE is inesperto then newDE is visibile;
if L is medio && C is insaturo && DE is esperto then newDE is inesperto;
if L is medio && C is insaturo && DE is inesperto then newDE is visibile;



